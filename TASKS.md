# Tasks

Every request, written down as it arrives. Tick off only after verifying.

## Open

- [ ] **Deploy.** The live site is behind: it still serves the pre-`readingRank`
      ranking (jusrin.dev/dict gives シン 寝 新 for "shin") and the old floated
      clear button. Nothing below is live until pushed.

## Done

- [x] QR code to https://jusrin.dev/dict/ — in the page and the README,
      generated by a dependency-free Python encoder, verified against qrencode
- [x] Hover any hanzi for its Chinese reading and meaning; click to open it

- [x] Chinese prioritised: CN ↔ EN is the default tab, Chinese wins ties in All
- [x] `what` → 什麼 first (segmented word counts + homograph splitting);
      `thank you` → 謝謝 first; `shin` → 新 kept first
- [x] History panel has one scroller, not two
- [x] Tabs, History and arrows all 38px, matching the checkbox pill
- [x] ← → history buttons left of History

- [x] Background redraws (examples arriving, filter toggled) no longer reset
      the selection or rewrite the URL

- [x] Selection is in the URL (`#all/doumo/jp:140170`), so a hard refresh or a
      shared link comes back to the same word

- [x] List fills the column — fixed-height app shell; the page no longer
      scrolls, the list and the definition each scroll in their own pane and
      end level (verified: both bottoms at 457 of a 469px viewport)
- [x] Back restores the selected row, not just the query (position first,
      entry identity as fallback)
- [x] `/` focuses the search box; typing `/` inside it is not hijacked

- [x] List + detail view — compact rows (64px, one-line clipped gloss) beside a
      detail pane; one pane at a time under 720px with a Back button
- [x] Back/forward navigation — typing pushes nothing (verified: 4 keystrokes
      add 0 entries); Enter and following a link each push one
- [x] Saved lookups — persisted in localStorage, History button in the header,
      click to reopen, Clear to empty; survives reload

- [x] Clear button is a flex sibling, not absolutely positioned over the input
      — `position: static`, text inset 15px left, button 11px from the right,
      vertical offset 0.00, no overlap
- [x] `shin` / しん ranks 新 first (`readingRank`; gloss depth only applies to
      English matches)
- [x] JP ↔ CN tab
- [x] Hanzi match first, English pivot labelled "eng heuristic"
- [x] Shinjitai → kyūjitai via Unihan, so 図書館 reaches 圖書館
- [x] Filter low-value entries; "Hide low scoring" checkbox, on by default,
      anchored right
- [x] Tabs All / JP ↔ EN / CN ↔ EN / JP ↔ CN; "all", never "omni"
- [x] Tab key cycles tabs, Shift+Tab reverses, focus stays in the box
- [x] Copy buttons per card
- [x] 中 → CN in all UI text
- [x] `糞` above `くっそ` for "shit" — ordering, not summed bonuses
- [x] Live reload in `npm run serve`; `npm run clean` instead of manual `rm`
- [x] Site moved to repo root for GitHub Pages
- [x] Survive mixed-vintage cached assets instead of throwing
- [x] Notes in AGENTS.md, not README.md

## Declined

- In-page IME for CJK input — no browser API can switch the keyboard; user
  decided against building one.
